import "package:flutter_test/flutter_test.dart";
// import "fil der skal testes" her

// Unit tests tester simple funktioner eller klasser

// Man kan bruge Given, When, Then struktur
// F.eks.: Given user with age 24, When birthday() is called, Then user is old

// Man kan lave mocks som "simulerer" hvad systemet skal kunne. F.eks. hvis man kalder en API eller database, skal man have internetforbindelse --> man tester med en "mock" i stedet


void main() {

  test("Første unit test", () => null);
  // Setup / ARRANGE

  // do / ACT

  //test / ASSERT
  // expect(actual, matcher);


}

